<runner>
  program bin/paster serve deploy.ini
  daemon on
  transcript mta.log
  # Enable this to run the child process as a different user
  # user mta
</runner>

<eventlog>
  <logfile>
    path ./zdaemon.log
  </logfile>

  <logfile>
    path STDOUT
  </logfile>
</eventlog>

